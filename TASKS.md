# Idel-DreamMaker 工作任务清单

> 跨版本工作追踪。新 AI 对话请先读此文件，找到当前未完成的任务。

---

> **重要背景：** 创作者本人不具备编程技术背景，本项目完全通过 vibe coding 方式制作。
> AI 在生成代码/内容/文档时需考虑这一前提——输出应自解释、可维护、避免不必要的复杂度。
> 所有涉及"作者操作"的流程（如写 .md 文件、跑命令等）需给出清晰步骤和预期结果。

---

新对话启动流程详见 `CLAUDE.md` → **「工作规范」** 章节。

---

## v0.3.6 — 标题栏精简 + 底部状态条完善

> 当前进度：✅ 全部完成

- Task 1 ✅ — 标题栏只留 [×]
- Task 2 ✅ — 底部状态条完善
- Task 3 ✅ — 迷你展开尺寸+居中修复
- Task 4 ✅ — 托盘 tooltip 格式更新

---

## v0.3.7 — 副本引擎与废土填充

> **当前阶段：v0.3.7（正在进行）**
>
> 核心目标：构建时 .md 解析 + 副本内容保护 + 废土副本完整填充
>
> 注意：取消原计划的玩家侧 AI 生成/导入导出功能，副本改为作者闭源制作。
> 副本数据在构建时（build.rs）解析并序列化为二进制嵌入，运行时不可直接提取。

| Task | 内容 | 涉及文件 |
|------|------|---------|
| **1** | **Cargo 依赖 + build.rs 框架** | `Cargo.toml` (+bincode), `build.rs` (新建) |
| **2** | **.md 解析器**：YAML frontmatter + 3 种 Markdown 表格解析 | `scenario.rs` |
| **3** | **build-time 序列化**：.md → bincode → `scenarios_data.rs`（自动生成，不进版本库） | `build.rs`, `scenarios_data.rs` |
| **4** | **运行时反序列化**：从生成字节加载 Scenario | `scenario.rs` |
| **5** | **内容校验**：ID 唯一、Level 升序、weight 1-10、文本 30-80 字、必填项非空 | `build.rs` |
| **6** | **核心引擎测试**（5 个）：等级计算、称号查找、事件过滤、成就检测、权重随机 | `scenario.rs` (末尾 `#[test]`) |
| **7** | **前端错误处理**：所有 `catch(e){}` 改为 toast 或日志输出 | `main.js` |
| **8** | **废土 500 事件**：wasteland.json → wasteland.md，扩充到 500 条 | `scenarios/wasteland.md` |

> 执行顺序：先 1-7（基础架构 + 测试 + 修复），再 8（内容填充）

---

## v0.3.8 — 节假日系统

> 规划中，未开始。

| Task | 内容 |
|------|------|
| 1 | 日期检测模块（chrono 匹配月/日） |
| 2 | 全球约 25 个通用节日事件池 |
| 3 | 废土副本定制节日事件 |
| 4 | 虚构游戏内节日（2-3 个，运行时随机分配日期） |

---

## v0.4.0 — 体验打磨

> 规划中，未开始。合并原 v0.4.5 打磨版本。
>
> 注：原 v0.4.0（AI 生成 + 导入导出）已废弃，改为体验打磨。

| Task | 内容 | 说明 |
|------|------|------|
| 1 | **UI 动效** | 弹窗 fade-in/slide-in；面板过渡动画；EXP 进度条平滑；日志条目淡入 |
| 2 | **全局错误 Toast** | 所有后端命令失败时前端 toast 提示（替代静默 catch） |
| 3 | **首次启动引导** | 新用户/干净存档时显示 1-2 屏简短指引 |
| 4 | **存档兼容** | JSON 格式变更时自动迁移（`serde(default)` 已有，补充迁移逻辑） |
| 5 | **英文完整覆盖** | 检查 UI 所有文本字段，补充缺失的 en 翻译 |
| 6 | **标题栏/状态条视觉统一** | 补全细节对齐、间距一致性 |

---

## 规划（待定）

以下内容暂未排入路线，视后续需求决定是否推进：

| 版本 | 内容 | 说明 |
|------|------|------|
| v0.4.5+ | 新副本（中世纪/赛博/修仙/克苏鲁） | 如需加入则逐个 .md 制作，每个 30-500 事件 |
| v0.6.0 | Mac 适配 | 菜单栏适配 + 字体路径 + 打包测试 |
| v1.0 | Steam 上架 | Steamworks 集成 + 云存档 + 商店页面上架 |

---

## 已废弃计划

以下内容曾在旧路线中规划，现已确认取消：

| 版本 | 原计划 | 原因 |
|------|--------|------|
| v0.4.0 | AI 生成 + 导入导出 + API Key 设置 | 不再开放玩家自制副本，改为闭源制作 |
| v0.4.5 | PDF/Word 上传支持 | 同上，不再需要用户侧文件导入 |

---

## 验证标准

每个 Task 完成后：
```
cargo test          # Rust 测试全过
cargo check         # 编译通过
npx vite build      # 前端构建通过
```

版本结束时额外：
```
cargo tauri build   # 打包验证
```

---

所有工作规范详见 `CLAUDE.md` → **「工作规范」** 章节。
